source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# rack-interface

gem 'bundler'
gem 'byebug'
gem 'logger'
gem 'puma'
gem 'rack'
gem 'rubocop', require: false
